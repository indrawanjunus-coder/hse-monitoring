Password comparison uses === (timing side-channel)
File
artifacts/api-server/src/lib/auth.ts:81
Evidence
return computed === hash;
Risk
Early-exit string compare lets a remote attacker infer hash bytes from response timing over many requests. Low exploitability in practice, trivial to fix.
Fix
crypto.timingSafeEqual(Buffer.from(computed, "hex"), Buffer.from(hash, "hex")) with a length check first.
M2Unauthenticated POST /api/company-payments/public-submit with no rate limit [API6:2023 · BLA-10 Resource Quota]
File
routes/company-payments.ts:50
Risk
Accepts arbitrary companyId from body, uploads 10 MB file to Google Drive, inserts a pending payment row. No rate limit, no CAPTCHA, no auth. Anonymous user can flood Drive quota, pollute payments table, spam sysadmin review queue, submit fraudulent proofs for competitors.
Fix
Rate-limit per IP (e.g. express-rate-limit, 5/hour), require CAPTCHA, or restrict to company.status='pending' only. Log IP + user-agent.
M3No input validation on any route (except /health) [A05:2025 · A10:2025 · API8:2023 · ASVS V5]
Files
All 25 route files except health.ts
Evidence
const { foo, bar } = req.body; + parseInt(req.params.id) everywhere. @workspace/api-zod exists in the repo but is imported by zero routes.
Risk
No type guarantees, no shape enforcement. parseInt() → NaN → silent 404s; missing required fields propagate undefined into the DB and fail later with obscure errors; malformed payloads can crash handlers (DoS).
Fix
Adopt Zod schemas via middleware. The @workspace/api-zod package is already generated from the OpenAPI spec — wire it up.
M4session_replication_role = 'replica' in restore disables FK checks
File
routes/backup.ts (SQL export path emits this line into every dump)
Risk
During restore, every FK constraint is suspended. If the dump was built from an inconsistent state, the restored DB is also inconsistent — orphan incidents without users, inspection_answers referencing deleted questions. Won't fail until a runtime query hits the orphan.
Fix
Re-enable FKs at end of import (SET session_replication_role = 'origin';). Wrap restore in a transaction. Warn on orphans found at import time.